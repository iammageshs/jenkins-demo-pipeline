steps:
  # Step to fetch code from Git repository
  - name: 'gcr.io/cloud-builders/git'
    args:
      - 'clone'
      - '--branch=main'
      - '--single-branch'
      - 'https://github.com/iammageshs/jenkins-demo-pipeline.git'
      - 'repo'

  # Add additional build steps here as needed
